[package]
name = "intent-escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0.0" # Asegurate de usar la versión más reciente de la red

[profile.release]
opt-level = "z"
overflow-checks = true